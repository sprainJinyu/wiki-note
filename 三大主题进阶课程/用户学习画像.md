# 用户学习画像（并发相关）

> 基于对话中观察的思考习惯、思路特点与学习方式，仅做客观记录。

## 思考习惯

- **极度重视最后 1% 的细节**：明确意识到「看似很小的表达偏差，可能导致思路偏移十万八千里」。会主动承认「方向没错，但细节表达有问题」，并立即修正。
- **自我纠错能力强**：能快速接受官方口径并用自己的话重新表达。
- **喜欢先建立背景再追问机制**：先把整体模型讲清楚，再追问哨兵、CAS 全称、自旋是不是同一回事。
- **倾向用已有概念去套新缩写**：会先猜 AQS 的 S 是锁、CAS 是自旋锁，再校准到官方全称。
- **对「成功」一词很敏感**：会把原子性说成「一定成功、没人干扰」，被指出后能把「这一次原子」和「整段历史」拆开。
- **会主动用版本号去补 CAS 的盲区**，再确认基本 CAS 本身不带版本。

## 当前学习状态

- 并发主线底层基石 C01～C06 已 100% 完成，能用自己的话准确复述各卡「今天只带走这一条」。
- 已区分：CAS = Compare-And-Swap；自旋锁是失败后循环再试的策略；AQS 的 S 是 Synchronizer。
- 已明确：基本 CAS 只比当前值；成功只说明这一次比较和写是原子的，不说明中间没人动过。
- 已明确：ABA 要用值旁边的版本来感知；版本是防 ABA 的手段，不是基本 CAS 的定义。
- 学习规划已升级为 15 张进阶卡组（C01~C15）：先走路线 1（JDK 核心工具储备 C07~C11），再切路线 2（现代实战与排查 C12~C15）。

## 业务需求与技术偏好

- **核心业务场景**：
  - 场景 A：接口多下游并行 RPC/DB 聚合、超时控制与快速降级（对应 `CompletableFuture`）；
  - 场景 C：微服务 TraceId、多租户上下文在线程池复用下的安全透传与防污染（对应 `TransmittableThreadLocal`）；
  - 场景 D：自研重试框架（`retryable`）与弹性隔离组件设计（对应退避 Jitter 与线程池隔离 Bulkhead）。
- **实战检验需求**：具备线上突发事故（CPU 100%、线程池堆积打满、死锁）下的 `top -Hp` / `jstack` 快速定界直觉。

## 引导建议（内部记录）

- 严格遵循 20 分钟通勤切片原则，每张卡只锁定一条核心判断。
- 阶段 2 重点打牢线程池进池机制与阻塞队列选型；阶段 3 紧密结合 `member-user-center` 的微服务与重试场景进行案例化串联。
