---
title: 元-工作线程Worker
created: 2026-08-29
updated: 2026-08-29
tags: [元知识点, 线程池, C07]
---

# 元-工作线程Worker

线程池里的实体只有一种：工作线程（Worker）。  
没有「核心线程类」和「非核心线程类」。

核心 / 非核心是按 [[元-core与max门槛]] 比出来的逻辑身份，不是两种线程。

## 巧思 / 细则

新建 Worker 时，当前这条任务可以当它的第一个任务直接跑，不用先入队再出队。  
跑完之后回到循环里，再从队列 `take` 取活。
