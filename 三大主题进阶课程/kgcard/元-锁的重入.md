---
title: 元-锁的重入
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, 锁, 重入]
---

# 元-锁的重入

两者（`synchronized` 与 `ReentrantLock`）在重入上没有区别。  
都支持持有锁的线程再次获取同一把锁，内部都维护了一个计数器。  
每次成功获取 +1，每次释放 -1，减到 0 才真正释放锁。
