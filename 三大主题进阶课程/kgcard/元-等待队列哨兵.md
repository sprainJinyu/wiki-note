---
title: 元-等待队列哨兵
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, AQS, 哨兵]
---

# 元-等待队列哨兵

`head`、`tail` 是 AQS 对象自己的两个指针，不长在哨兵 Node 里面。  
`head` 常常指向一个不代表等待线程的空 Node，这就是哨兵。

哨兵与后面那些等待节点不是一类。它要解决的是「队列还在不在」：第一次有人失败要排队时先放一个空 Node，之后入队都接在当前 tail 后面。  
它不省「一个还是多个等待者」的判断，也不让 `head.next` 永远非空。
