---
title: 元-中断获取与超时获取
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, 锁, 中断, 超时]
---

# 元-中断获取与超时获取

超时：自己控制的行为。设定最长等待时间，时间到了还没拿到锁就返回 false，自己继续往下做。与别人无关。  

中断：别人通知给我们。其他线程持有本线程的引用，调用 `interrupt()` 方法，本线程在等待过程中感知后抛出中断异常，不再继续等待。  

两者都能「不等死」，但触发机制不同：一个是自己决定等多久，一个是别人主动通知。
