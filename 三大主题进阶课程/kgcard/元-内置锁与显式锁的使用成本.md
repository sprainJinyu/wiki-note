---
title: 元-内置锁与显式锁的使用成本
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, 锁, 使用成本]
---

# 元-内置锁与显式锁的使用成本

内置的 JVM 锁（`synchronized`）使用成本更低：代码块结束时会自动释放，管理成本特别低，用起来更省心。  

工具类提供的 `ReentrantLock` 使用成本高一些，需要手动调用 `unlock()`（通常写在 `finally`），但功能更丰富。
