---
title: 元-state在可重入锁中的双重含义
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, AQS, state]
---

# 元-state在可重入锁中的双重含义

可重入锁用一个 `state` 同时做两件事：

1. 是否抢到了锁（有没有人在用）。  
2. 当前这个线程获取同一把锁的次数（重入计数）。

`state == 0` 空闲；`state > 0` 表示已有人拿着，数值就是重入次数。
