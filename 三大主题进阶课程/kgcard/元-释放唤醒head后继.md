---
title: 元-释放唤醒head后继
created: 2026-08-26
updated: 2026-08-26
tags: [元知识点, AQS]
---

# 元-释放唤醒head后继

释放锁时不去找「当前持锁线程」（他不在队列里），而是唤醒 `head` 后面那一个。  
`head.next` 仍可能为空：没人在等就不唤醒。动作统一，但返回值仍要判空。

公平性不改唤醒对象。公平性改的是：队列里的人醒之前，新来还没入队的线程有没有机会先试一把锁。
